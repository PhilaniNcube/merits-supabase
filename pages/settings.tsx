import Layout from "../components/Layout";
import Settings from "../components/Settings";

const settings = () => {
  return <Layout>
          <Settings />
         </Layout>;
};
export default settings;
